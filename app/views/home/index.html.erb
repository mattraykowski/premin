<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<% if is_root_domain? %>
  <% if user_signed_in? %>
    <%= link_to "Go To My Site", root_url(subdomain: current_user.account.subdomain) %>
  <% end %>
<% end %>
