<div class="row-fluid">
  <div class="span3">
    <%= render partial: "shared/page_sidebar", locals: { page_collection: @sidebar_pages } %>
  </div>
  <div class="span9">
    <h3><%= @page.title %></h3>
    <div class="span12"><%= @page.content.html_safe %></div>
    <%= link_to 'Edit', edit_page_path(@page) %> |
    <%= link_to 'Back', pages_path %>
  </div>
</div>

